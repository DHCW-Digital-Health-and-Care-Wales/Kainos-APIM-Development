get:
  tags:
    - Health
  operationId: ping
  summary: Simple health check endpoint
  description: Returns a simple response to verify the API is responding
  responses:
    '200':
      $ref: '../components/responses/PingOk.yaml'